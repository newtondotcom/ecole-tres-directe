import { fullProcedure } from "@/src/lib/temp";


export default async function RemplirAppreciations() {
    // Ajouter un prompt pour customiser l'appréciation

    // Faire un batch job pour les 30 élèves 

    await fullProcedure();
    
    return (
        <div>
            <h1>Remplir Appréciations</h1>
        </div>
    )
}