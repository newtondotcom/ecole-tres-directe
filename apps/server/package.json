{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@etd/api": "workspace:*",
    "@etd/env": "workspace:*",
    "@hono/trpc-server": "^0.4.0",
    "@trpc/server": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@etd/config": "workspace:*",
    "@types/bun": "^1.2.6",
    "@types/node": "^24.10.1",
    "tsdown": "^0.16.5",
    "typescript": "^5.8.2"
  }
}
